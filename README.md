# create-codes monorepo

This is monorepo for creating new front-end codes with a standard structure.

## Examples

- `react`: React app with a standard structure

## Packages

- `eslint-config-custom`: `eslint` configurations
